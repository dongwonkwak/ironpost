//! Module registry and initialization.
//!
//! This module is intentionally minimal now that we use PluginRegistry from core.
//! Individual modules are initialized directly in the orchestrator.

// Empty module - all functionality moved to PluginRegistry in core
